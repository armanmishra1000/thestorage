// Frontend/src/environments/environment.prod.ts
export const environment = {
  production: true,
  apiUrl: 'https://your-backend-url.onrender.com/api/v1', // We will replace this later
  wsUrl: 'wss://your-backend-url.onrender.com/ws_api/ws' // We will replace this later
};